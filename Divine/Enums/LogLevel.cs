namespace Divine.Enums
{
    public enum LogLevel
    {
        OFF = 0,
        FATAL = 100,
        ERROR = 200,
        WARN = 300,
        INFO = 400,
        DEBUG = 500,
        TRACE = 600,
        ALL = 1000
    }
}
